import { FC } from "react";
import CreateCustomersScreen from "@/screens/CreateCustomerScreen";

type TIndexProps = {};
const Create: FC<TIndexProps> = () => {
  return <CreateCustomersScreen />;
};

export default Create;
