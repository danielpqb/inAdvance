import CreateDBScreen from "@/screens/CreateDBScreen";
import { FC } from "react";

type TIndexProps = {};
const Create: FC<TIndexProps> = () => {
  return <CreateDBScreen />;
};

export default Create;
